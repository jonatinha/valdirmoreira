---
import "@/styles/globals.css";
import { Image } from "astro:assets";

import { Phone, Facebook } from "lucide-react";

import { Button } from "@/components/ui/button";
---

<div
    class="w-full bg-white bg-background p-12 gap-2 flex flex-col justify-end items-end relative"
>
    <Image
        src="/nomelogocida.png"
        alt="Valdir Moreira e Cidadania 23."
        width="100"
        height="100"
        class="absolute left-[-2rem] top-20"
    />
    <h2
        class="scroll-m-20 text-4xl font-extrabold tracking-tight lg:text-5xl text-pink-500"
    >
        Contato
    </h2>

    <p class="text-end text-balance gap-2 flex flex-col">
        <div class="pl-10 text-end">
            Estamos aqui para ouvir você! Se tiver dúvidas, sugestões ou quiser
            saber mais sobre nossas propostas e projetos, entre em contato
            conosco!
        </div>
    </p>

    <div class="pl-14 gap-2 flex flex-col w-full pt-6">
        <Button className="bg-green-500 text-lg w-full">
            <Phone className="mr-2 h-6 w-6" /> WhatsApp
        </Button>
        <Button className="bg-blue-500 text-lg w-full">
            <Facebook className="mr-2 h-6 w-6" /> Facebook
        </Button>
    </div>
</div>
